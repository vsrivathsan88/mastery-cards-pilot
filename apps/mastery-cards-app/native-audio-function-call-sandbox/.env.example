# Mastery Cards App - Environment Variables
# ==================================================
# For Local Development: Copy this file to .env.local and fill in your keys
# For Vercel Production: Set these in Vercel dashboard (Project Settings → Environment Variables)

# Required: Gemini API Key for voice AI interaction
# Get from: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# NOT NEEDED in .env.local (frontend doesn't use it directly)
# Claude API Key is ONLY set in Vercel dashboard as CLAUDE_API_KEY (without VITE_ prefix)
# This keeps it secure on the server-side
# Get from: https://console.anthropic.com
# Set in Vercel: CLAUDE_API_KEY=your_claude_api_key_here

# ==================================================
# REMOVED: No longer needed with Vercel Serverless deployment
# - VITE_WS_SERVER_URL (using client-side orchestration)
# - VITE_BACKEND_URL (using Vercel serverless functions at /api/*)
# - VITE_CLAUDE_API_KEY (moved to server-side CLAUDE_API_KEY)
